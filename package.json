{
  "name": "deck-engine",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @deckio/deck-engine",
    "build": "npm run build -w @deckio/deck-engine --if-present",
    "test": "npx vitest run",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
