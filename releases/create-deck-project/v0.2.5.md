# create-deckio v0.2.5

**Released:** 2026-03-21

## What changed

### Changed
- Show the `@deckio/deck-engine` version in the CLI startup screen before scaffolding begins.
- Pin generated projects to the bundled `@deckio/deck-engine` release instead of an unqualified `latest` fallback.

### Fixed
- Skip temporarily disabled engine skills during pre-install asset copying so fresh decks match the post-install engine sync state.

## Upgrade guide

No action required.

## Contributors

- GitHub Copilot