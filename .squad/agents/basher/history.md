# Project Context

- **Owner:** Ali Soliman
- **Project:** DECKIO / deck-engine — a presentation engine and scaffolder for creating beautiful, Copilot-powered slide decks
- **Stack:** Node.js monorepo (npm workspaces), Vite, HTML/CSS/JS, packages: `@deckio/deck-engine` and `@deckio/create-deck-project`
- **Created:** 2026-03-13

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- **2026-03-13 Team Kickoff:** Zero dev scripts exist; `npm run dev` missing entirely. Export code duplicated across browser/export and cli/pdf; consolidation into shared utility is Phase 1 priority. Bundling solid but no dev loop ergonomics. CLI export driver works but lacks error handling and progress indicators.
- **Team Findings (Rusty synthesis):** SlideProvider handles state persistence elegantly; Vite integration dedupes react/react-dom via subpath import magic. Copilot skills are file-driven (no code plugins). Export uses modern-screenshot for slides + Puppeteer for PDF assembly. No generalized plugin API yet; extensibility via React composition.
- **Phase 1 Roadmap:** Implement dev/build/preview scripts, consolidate export logic, add CLI error handling. Phase 1 is dev workflow + Vitest infrastructure (Linus concurrent). Phase 2 layout + animation (Livingston). Phase 3 polish + presenter mode.
- **Phase 1 Dev Workflow (done):** Root scripts added (dev/build/test/lint) via npm workspace delegation. Engine ships raw .jsx — no build step needed, `build` uses `--if-present`. `prepublishOnly` added to engine. Scaffolder pinned engine dep `^1.7.7` → `^1.8.2`. `capture-screen.mjs` was Windows-only (hardcoded Edge path) — now cross-platform. Stale `.tgz` removed and `*.tgz` added to `.gitignore`. Key paths: root `package.json` (scripts), `packages/deck-engine/package.json` (prepublishOnly), `packages/deck-engine/scripts/capture-screen.mjs` (browser detection).
- **Team Update (2026-03-13T00:58:09Z):** Linus completed Vitest setup — 33 tests now passing. `prepublishOnly` gate is now live and meaningful (will run tests on publish). Saul's design audit found no CSS in create-deckio; Phase 2 token work is scoped entirely to deck-engine. DEV-001 filed in decisions.md.
- **Tailwind CSS v4 + Theme Architecture (2026-03-13T01:45:00Z):** Installed `tailwindcss` + `@tailwindcss/vite` in deck-engine. Vite plugin (`vite.js`) now exports `deckPlugin()`, `deckPlugins()` (combo), and `tailwindPlugin()`. Created `themes/` directory with pluggable theme system: `dark.css`, `light.css`, `shadcn.css` — each has CSS custom properties + `@theme inline` bridge for Tailwind utility generation. Theme loader (`themes/theme-loader.js`) provides `resolveTheme()` and `getAvailableThemes()`. New themes = drop a CSS file. Scaffolder updated: `deck.config.js` now has `theme` field (defaults to "dark"), CLI asks for theme during onboarding, `main.jsx` imports theme CSS from engine. Generated projects include `tailwindcss` + `@tailwindcss/vite` in devDeps. Backwards compatible — existing decks without `theme` field still work. 49 tests passing (was 33). THEME-001 filed.
- **Bug Fix: tailwindPlugin export + CLI polish (2026-03-14):** Root cause of `tailwindPlugin` SyntaxError: published npm v1.8.2 predated the theme work, so downstream projects got a `vite.js` without `tailwindPlugin`. Fixes: (1) Bumped engine version `1.8.2` → `1.9.0` to reflect new tailwind/theme exports. (2) Added `"./vite.js": "./vite.js"` to exports map so both `@deckio/deck-engine/vite` and `@deckio/deck-engine/vite.js` resolve correctly. (3) Updated scaffolder dep `^1.8.2` → `^1.9.0`. CLI UX overhaul: ANSI-colored prompts (`fmt` helpers), default accent changed `#3fb950` → `#6366f1` (indigo-500, more modern), accent color swatch preview via true-color ANSI, theme picker shows descriptions (deep space / projection / editorial neutral), styled success output. No external deps added (no gum). 94 tests passing.
- **Critical Bug Fix: Production build "dirname" error (2026-03-14):** `index.js` re-exported `getAvailableThemes`, `resolveTheme`, `DEFAULT_THEME`, `BUILTIN_THEMES` from `themes/theme-loader.js`, which imports Node.js builtins (`fs`, `path`, `url`). Rollup can't resolve these browser-side during production builds — dev server hid the issue because Vite doesn't bundle in dev mode. Fix: (1) Removed theme-loader re-exports from `index.js` — browser entry now exports only React components, hooks, and constants. (2) Added re-exports of all theme-loader utilities (`resolveTheme`, `getAvailableThemes`, `DEFAULT_THEME`, `BUILTIN_THEMES`) from `vite.js` so Node.js consumers access them via `@deckio/deck-engine/vite`. Theme-loader also remains directly importable via `@deckio/deck-engine/themes/theme-loader`. **Key lesson:** Never re-export Node.js-only modules from the main browser entry point. The browser/Node boundary must be enforced at the exports map level. 94 tests passing, production build verified with scaffolded project.
- **Critical Bug Fix: Scaffolder version drift / ETARGET (2026-03-14):** `utils.mjs` hardcoded `'@deckio/deck-engine': '^1.9.0'` but v1.9.0 was never published (latest npm = 1.8.2). Every scaffolded project failed `npm install`. Root cause: version bump in engine package.json without npm publish, and scaffolder used a hardcoded string instead of reading dynamically. Fix: (1) Reverted engine version `1.9.0` → `1.8.2`. (2) Added `resolveEngineRef(projectDir)` to `utils.mjs` — reads the engine's actual version from its `package.json` at scaffold time. (3) In local dev (monorepo), generates `file:` protocol references so npm install resolves from the local filesystem, bypassing the registry entirely. (4) `packageJson()` now accepts optional `engineRef` parameter; defaults to `resolveEngineRef()`. (5) Cleaned up stale test project. 7 new tests for resolveEngineRef. 101 tests passing. E2E verified: scaffold → npm install → vite dev → vite build all pass. **Key lesson:** Never hardcode dependency versions in scaffolder templates. Always read from the source package.json. For local dev, use `file:` protocol to decouple from npm publish status. Second scaffolder bug that shipped — must add E2E verification to publish workflow.
- **shadcn/ui Design System Integration (2026-03-14):** Added opt-in `designSystem` field to scaffolder (`"none" | "shadcn"`, default `"none"`). Separate from `theme` — you can use theme `"shadcn"` without the component system, or (default) get both. CLI asks "Include shadcn/ui components?" when theme is `"shadcn"`, defaults Yes. When enabled: (1) `packageJson()` adds `class-variance-authority`, `clsx`, `tailwind-merge` as deps. (2) Generates `components.json` with `rsc: false`, `tsx: false` (JSX project, no RSC). (3) Creates `src/lib/utils.js` with `cn()` utility (clsx + tailwind-merge). (4) Creates `jsconfig.json` with `@/*` → `./src/*` path alias (required by shadcn CLI). (5) `viteConfig()` adds `resolve.alias` mapping `@` → `src/`. (6) `deckConfig()` includes `designSystem: 'shadcn'` field. (7) README gets shadcn usage section. (8) Creates empty `src/components/ui/` directory. Non-interactive mode reads `DECK_DESIGN_SYSTEM` env var, defaults to `"shadcn"` when theme is `"shadcn"`. Non-shadcn themes get zero shadcn artifacts. `VITE_CONFIG` constant replaced with `viteConfig()` function for conditional alias generation. **Key lesson:** shadcn CLI requires `jsconfig.json` (or `tsconfig.json`) for path resolution — without it, `npx shadcn add` fails with "Couldn't find tsconfig.json". Pre-generating files instead of running `npx shadcn init` is more reliable and faster. 130 tests passing (was 101). Full E2E verified: scaffold → npm install → dev server → production build → `npx shadcn add button` → button renders in slide → build succeeds with button import.
- **CLI TUI Overhaul + Color Presets + ReactBits (2026-03-14):** Three CLI improvements shipped in one pass: (1) **@clack/prompts TUI** — replaced raw readline prompts with `@clack/prompts` for modern styled CLI. Uses `intro()`, `text()`, `select()`, `confirm()`, `spinner()`, `outro()`. Handles cancel (Ctrl+C) gracefully with `isCancel()` checks. Non-interactive mode (piped stdin, env vars) still works — `process.stdin.isTTY` gating preserved. `@clack/prompts` added as the first runtime dependency of create-deckio. (2) **Color preset picker** — `COLOR_PRESETS` array (8 curated colors: Indigo, Emerald, Rose, Amber, Cyan, Violet, Orange, Blue) exported from `utils.mjs`. Displayed as `clack.select()` with true-color ANSI swatches. "Custom hex" option at the end for freeform input with validation. Default: Indigo (#6366f1). (3) **ReactBits registry** — `componentsJson()` now includes `registries: { "@react-bits": "https://reactbits.dev/r/{name}.json" }`, enabling `npx shadcn add @react-bits/animated-content` in scaffolded projects. Zero architecture change — uses shadcn's built-in registry protocol. 136 tests passing (was 130, +6 new for COLOR_PRESETS and registries). Full E2E verified: both interactive and non-interactive scaffold → npm install → dev server (HTTP 200) → production build all pass.
- **Dark/Light Mode for shadcn Design System (2026-03-15):** Added dark mode support to the shadcn theme path using shadcn's official Vite pattern (.dark class on `<html>` + CSS variables). (1) **`shadcn.css` updated** — added `.dark` block with full shadcn dark oklch palette (core semantic colors, overlays/glows, chart colors, popover). Light stays in `:root`, dark activates via `.dark` class. Both use `color-scheme` property. (2) **ThemeProvider scaffolded** — `themeProviderJsx()` in `utils.mjs` generates `src/components/theme-provider.jsx`. React context managing light/dark/system state with localStorage persistence (key: `deckio-ui-theme`). Adds/removes `.dark` class on `<html>`. Respects `prefers-color-scheme` for system mode. (3) **ModeToggle scaffolded** — `modeToggleJsx()` generates `src/components/mode-toggle.jsx`. Fixed-position button (bottom-right) cycling light→dark→system. Inline SVG icons (sun/moon/monitor). No external deps. (4) **App.jsx now conditional** — `APP_JSX` constant replaced with `appJsx({ designSystem })` function. When `designSystem: "shadcn"`, wraps with `<ThemeProvider defaultTheme="light">` and includes `<ModeToggle />`. Non-shadcn projects unchanged. (5) **Scaffolder updated** — `index.mjs` writes `theme-provider.jsx` and `mode-toggle.jsx` when `designSystem === 'shadcn'`. (6) **Only for shadcn** — dark/light themes don't get ThemeProvider or ModeToggle (they're single-mode). `data-theme` attribute (from SlideProvider) stays constant as theme identity; `.dark` class is the mode within that theme. 159 tests passing (was 136, +23 new: 6 themeProviderJsx, 5 modeToggleJsx, 7 appJsx, 5 shadcn.css dark mode). Full E2E verified: scaffold → npm install → production build → `.dark` block in compiled CSS → ThemeProvider+ModeToggle in bundle. Test project updated.
- **Bug Fix: Vite fs.allow for file: protocol refs (2026-03-15):** When using `file:` references for local dev, Vite's security sandbox blocks serving engine CSS files (global.css, theme CSS) because the symlinked `node_modules/@deckio/deck-engine` resolves outside the project root. Fix: added `server: { fs: { allow: ['..', '../..'] } }` to `viteConfig()` in `utils.mjs`. The `serverBlock` is always included — it's harmless in production (Vite ignores `server` config during `vite build`) and essential for local `file:` dev. Also patched the existing test project at `test-shadcn-deck/vite.config.js`. 161 tests passing (was 159, +2 new: server.fs.allow presence in both default and shadcn modes). E2E verified: scaffold → npm install → dev server → `global.css` 200 → `shadcn.css` 200 → zero errors in server log → clean teardown.
- **shadcn MCP Server Pre-configuration (2026-03-15):** Added `vscodeMcpConfig()` to `utils.mjs` — generates `.vscode/mcp.json` with the shadcn MCP server config (`npx -y shadcn@latest mcp`). Scaffolder writes this file when `designSystem === 'shadcn'`. README gets "AI-Powered Component Discovery" section with example prompts for ReactBits. CLI outro shows MCP confirmation message. Non-shadcn projects get nothing — zero MCP artifacts. Pre-generating the file instead of running `npx shadcn mcp init` (which is interactive). 217 tests passing (was 211, +6 new for vscodeMcpConfig). E2E verified: correct `.vscode/mcp.json` content, non-shadcn project has no `.vscode/mcp.json`.
- **CLI Onboarding Streamline — Design-System-Aware Color Selection (2026-03-15):** Fixed redundant UX where CLI asked for both accent color AND aurora palette for shadcn users. Reordered prompts: (1) Title, (2) Subtitle, (3) Icon emoji (moved up), (4) Design system, (5) Appearance, then branching: Default gets accent color picker, shadcn gets aurora palette picker (no accent step). Added `AURORA_ACCENT_MAP` and `auroraAccent()` to `utils.mjs` — derives accent from aurora palette's first color (Ocean→#0ea5e9, Sunset→#f97316, Forest→#10b981, Nebula→#8b5cf6, Arctic→#06b6d4, Minimal→#71717a). Non-interactive mode updated: `DECK_ACCENT` env var only used for default design system; shadcn derives accent from `DECK_AURORA_PALETTE`. 232 tests passing (was 220, +12 new for AURORA_ACCENT_MAP and auroraAccent). Key files: `index.mjs` (prompt flow), `utils.mjs` (AURORA_ACCENT_MAP, auroraAccent).
- **Bug Fix: Remove ModeToggle — Make appearance choice meaningful (2026-03-15):** The ModeToggle button let users override the dark/light appearance picked during onboarding, making that CLI choice pointless. Fix: (1) Removed `modeToggleJsx()` function from `utils.mjs`. (2) Removed `<ModeToggle />` and its import from `appJsx()` template — ThemeProvider still wraps the app and sets the correct `.dark`/`.light` class. (3) Removed `mode-toggle.jsx` write from `index.mjs` scaffolder. (4) Updated features slide copy: "Built-in mode toggle" → "Appearance via ThemeProvider". (5) Updated tests: removed 5 modeToggleJsx tests, replaced ModeToggle-inclusion test with ModeToggle-absence test. 227 tests passing. E2E verified: dark scaffold → `defaultTheme="dark"`, no toggle; light scaffold → `defaultTheme="light"`, no toggle. Users wanting a toggle later can add one via `npx shadcn add`. Key files: `utils.mjs` (modeToggleJsx removed, appJsx updated), `index.mjs` (import + file write removed), `__tests__/utils.test.js` (tests updated).
- **Scaffolder: Direct Asset Copy + Descriptor Pipeline Verification (2026-03-15):** Verified the full scaffolder → skills → instructions → descriptors pipeline end-to-end. Previously, skills/instructions copy relied entirely on `init-project.mjs` running after `npm install`. If npm install failed, the scaffolded project had no Copilot skills. Fix: (1) Added `copyEngineAssets(dir)` to `index.mjs` — resolves the engine source in the monorepo (`../deck-engine/`) and copies skills, instructions, and AGENTS.md directly during scaffolding, before npm install. (2) Added `"./themes/descriptors/*": "./themes/descriptors/*"` to engine exports map for explicit descriptor accessibility. (3) Fixed pre-existing test: `BUILTIN_THEMES` test now includes `funky-punk`. `init-project.mjs` still runs post-install for state.md, .github/eyes/, .vscode/settings.json, and stale-file cleanup. Both paths (default + shadcn) verified: 6 skills, 4 instructions, 3 descriptors, production build passes. 236 tests passing. Key files: `packages/create-deckio/index.mjs` (copyEngineAssets), `packages/deck-engine/package.json` (descriptors export), `packages/deck-engine/__tests__/theme-loader.test.js` (BUILTIN_THEMES fix).
